version: "3.5"
services:
  postgres:
    image: "postgres:12.1"
    ports:
      - "5432:5432"
    environment:
      - POSTGRES_USER=admin
      - POSTGRES_PASSWORD=1234
      - POSTGRES_DB=customerDB
